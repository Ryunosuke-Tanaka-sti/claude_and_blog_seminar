# ツール仕様書：Playwright MCP セットアップ

## 概要
Claude CodeからPlaywright MCPをnpx方式で利用できるようにするためのDevContainer環境セットアップツールです。ブラウザ自動化機能をClaude Codeに統合し、Webページのナビゲーション、スクリーンショット取得、要素操作などを可能にします。

## 背景・課題
- Claude Codeで Web UI テストやスクレイピングを行う際、手動でブラウザ操作する必要がある
- スクリーンショットの取得やページ解析を効率化したい
- DevContainer環境でヘッドレスブラウザを簡単に利用したい
- MCPサーバーの設定とブラウザエンジンのインストールを一度で完了させたい

## ツールの目的
- DevContainer環境にPlaywright MCPサーバーを構成
- npx方式による依存関係なしのシンプルなセットアップ
- Chromiumブラウザエンジンのインストールと動作確認
- GitリポジトリへのMCP設定の統合

## 入力仕様

### 環境要件
**必須環境**:
- Node.js: v20以上
- npm: v10以上
- DevContainer: Bullseye base image以降
- Claude Code: VSCode拡張機能がインストール済み

**検証方法**:
```bash
node --version  # v20.x.x以上
npm --version   # 10.x.x以上
```

### 既存ファイルの確認
**確認対象**:
- `.mcp.json` - 存在する場合は既存設定を保持して追加
- `.gitignore` - Playwright関連の除外設定を追記

### 入力エラー時の挙動
- Node.jsバージョンが不足している場合: エラーメッセージを表示して終了
- `.mcp.json`が不正なJSON形式の場合: バックアップを作成してから上書き提案
- DevContainerでない場合: 警告を表示して継続（ヘッドレスモードは維持）

## 処理内容

### 処理フロー
1. 環境確認（Node.js, npm, npxのバージョン確認）
2. `.mcp.json`の存在確認と読み込み
3. Playwright MCP設定を`.mcp.json`に追加または新規作成
4. Chromiumブラウザエンジンのインストール（`npx playwright install chromium`）
5. `.gitignore`へPlaywright関連ディレクトリを追加
6. セットアップ完了の確認とユーザーへの案内

### 設定内容

**`.mcp.json` の構成**:
```json
{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--headless", "--isolated"]
    }
  }
}
```

**設定パラメータの説明**:
- `type: "stdio"`: Claude Codeとの標準入出力通信
- `command: "npx"`: グローバルインストール不要のnpx実行
- `@playwright/mcp@latest`: 常に最新版を使用（開発環境）
- `--headless`: GUIなし環境（DevContainer）で必須
- `--isolated`: プロファイルロック問題を回避、メモリ上にプロファイル保持

### 条件分岐
- `.mcp.json`が存在しない場合: 新規作成
- `.mcp.json`に既存のMCPサーバー設定がある場合: `playwright`エントリを追加
- `.gitignore`が存在しない場合: 新規作成
- `.gitignore`に既存の設定がある場合: Playwright関連の除外設定を追記

## 出力仕様

### 出力ファイルの形式

**ファイル1: `.mcp.json`**
- **ファイルパス**: `/home/node/workspace/.mcp.json`
- **データ形式**: JSON
- **エンコーディング**: UTF-8

**ファイル2: `.gitignore`（更新）**
- **ファイルパス**: `/home/node/workspace/.gitignore`
- **追加内容**:
```gitignore
# Playwright MCP
.playwright-mcp/
.playwright-auth/
*.webm
trace.zip
```

### 出力ファイルの構造
```
/home/node/workspace/
├── .mcp.json                  # 新規作成または更新
├── .gitignore                 # 更新
└── ~/.cache/ms-playwright/    # Chromiumインストール先
    └── chromium-1194/         # バージョン番号は変動
```

### 出力エラー時の挙動
- `.mcp.json`作成失敗: 書き込み権限エラーを表示して終了
- Chromiumインストール失敗: ネットワークエラーまたは依存関係不足を表示
- `.gitignore`更新失敗: 警告を表示して継続（セットアップは完了）

## エラーハンドリング

### 想定エラーケース
| エラーケース | 発生条件 | エラーメッセージ | 処理 |
|------------|---------|----------------|------|
| Node.jsバージョン不足 | `node --version`がv20未満 | "エラー: Node.js v20以上が必要です（現在: vX.X.X）" | 終了コード1で終了 |
| npm未インストール | `npm --version`がエラー | "エラー: npmがインストールされていません" | 終了コード1で終了 |
| Chromiumインストール失敗 | ネットワークエラーまたは依存不足 | "エラー: Chromiumのインストールに失敗しました" | 再試行を提案 |
| プロファイルロックエラー | 既存ブラウザプロセスが動作中 | "警告: ブラウザプロファイルがロックされています" | キャッシュクリア手順を案内 |
| JSON構文エラー | `.mcp.json`が不正 | "エラー: .mcp.jsonの構文が不正です" | バックアップ作成を提案 |

### ログ出力
- **正常時**: "✅ Playwright MCPセットアップが完了しました"
- **警告時**: "⚠️ .gitignoreの更新に失敗しました（手動で追加してください）"
- **エラー時**: "❌ エラーが発生しました: [詳細]"

## 実行方法

### セットアップコマンド
```bash
# この仕様書に基づいてClaude Codeが自動実行
# ユーザーは以下の流れで依頼
```

**ユーザーの依頼内容**:
```
この仕様書を読み込んでPlaywright MCPをセットアップしてください：
@docs/features/tool-playwright-mcp-setup/spec.md
```

### 手動セットアップ（参考）
```bash
# ステップ1: .mcp.json作成
cat > .mcp.json << 'EOF'
{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--headless", "--isolated"]
    }
  }
}
EOF

# ステップ2: Chromiumインストール
npx playwright install chromium

# ステップ3: .gitignore更新
cat >> .gitignore << 'EOF'

# Playwright MCP
.playwright-mcp/
.playwright-auth/
*.webm
trace.zip
EOF
```

### 動作確認コマンド
```bash
# 環境確認
node --version && npm --version

# Chromiumインストール確認
npx playwright install --list

# MCP設定確認
cat .mcp.json
```

## パフォーマンス要件

### 処理速度の目標
- セットアップ全体: 5分以内（初回）
- Chromiumダウンロード: 2分以内（ネットワーク速度による）
- 設定ファイル作成: 10秒以内

### メモリ使用量
- Chromiumインストール時: 最大200MB
- ブラウザ起動時: 最大300MB（ヘッドレスモード）

### 最適化の考慮事項
- npxキャッシュ活用により2回目以降の起動を高速化
- `--isolated`フラグでプロファイルロック問題を事前回避

## 依存関係

### 使用するパッケージ
- `@playwright/mcp`: Playwright MCPサーバー（npx経由、インストール不要）
- `playwright`: Chromiumブラウザエンジン（自動ダウンロード）

### 外部サービス・API
- `https://playwright.azureedge.net/builds/`: Chromiumダウンロード元
- ダウンロードサイズ: 約173MB

## セキュリティ考慮事項

### 入力のサニタイズ
- `.mcp.json`読み込み時にJSON構文を検証
- ファイルパスはプロジェクトルートに限定（パストラバーサル対策）

### 機密情報の取り扱い
- `.playwright-auth/`を`.gitignore`に追加（認証情報の誤コミット防止）
- `.playwright-mcp/`を`.gitignore`に追加（キャッシュファイルの除外）

## テスト方法

### 動作確認の手順
1. VSCodeをリロード（`Cmd/Ctrl + Shift + P` → "Developer: Reload Window"）
2. Claude Codeを開く
3. 以下のテストプロンプトを実行:
   ```
   Playwright MCPを使用して、https://example.com を開いてページタイトルを取得してください
   ```

### 検証ポイント
- ✅ `.mcp.json`が正しく作成されているか
- ✅ Chromiumがインストールされているか（`~/.cache/ms-playwright/chromium-*/`）
- ✅ `.gitignore`にPlaywright関連の除外設定が追加されているか
- ✅ Claude CodeでPlaywright MCPツールが認識されているか
- ✅ `https://example.com`にアクセスしてタイトル取得が成功するか
- ✅ スクリーンショット取得が正常に動作するか

### テストデータ
```
テストURL: https://example.com
期待されるタイトル: "Example Domain"
期待されるスクリーンショット: .playwright-mcp/screenshot.png
```

## 実装時の注意点

### 優先度
- 🔴 必須: `.mcp.json`の作成または更新
- 🔴 必須: Chromiumブラウザのインストール
- 🔴 必須: `.gitignore`の更新
- 🟡 推奨: VSCodeリロードの案内
- 🟡 推奨: 動作確認手順の提示
- 🟢 オプション: トレース機能の設定追加

### 実装順序
1. 環境確認（Node.js, npm, npxのバージョンチェック）
2. `.mcp.json`の作成または更新
3. Chromiumブラウザのインストール
4. `.gitignore`の更新
5. ユーザーへのセットアップ完了案内とリロード手順の提示
6. 動作確認テストの実行方法を説明

### 既存コードとの関係
- 既存の`.mcp.json`に他のMCPサーバー設定がある場合は保持
- `docs/CLAUDE.md`のルールに従い、`docs/features/`で計画を立ててから実装

## AIへの依頼事項

```
以下の仕様書を読み込んでPlaywright MCPをセットアップしてください：
@docs/features/tool-playwright-mcp-setup/spec.md

実装方針：
- 環境確認を最初に実施
- 既存の.mcp.json設定は保持して追加
- エラーハンドリングを適切に実装
- セットアップ完了後、動作確認方法をユーザーに案内
```

## 制約事項

### 技術的制約
- Node.js バージョン: v20以上
- npm バージョン: v10以上
- Playwright バージョン: 最新（npx経由で自動解決）

### 環境制約
- DevContainer環境での実行を想定（`--headless`必須）
- インターネット接続が必要（Chromiumダウンロード）
- ディスク空き容量: 最低500MB（Chromium + キャッシュ）

## 参考資料
- Playwright MCP GitHub: https://github.com/microsoft/playwright-mcp
- Playwright公式: https://playwright.dev
- MCP仕様: https://spec.modelcontextprotocol.io/
- 参照手順書: `docs/spec/marp/image-size-guideline.md`（提供されたユーザー資料）

## 備考
- npx方式を採用することで、プロジェクトの`package.json`を汚染せず、常に最新版を利用可能
- 本番環境ではバージョン固定（`@playwright/mcp@0.0.47`）を推奨
- `--isolated`フラグにより、複数のClaude Codeセッションでも競合なし

---

## 仕様書レビューチェックリスト

実装前に以下を確認してください：

- [x] 入力データの形式が明確に定義されているか（環境要件）
- [x] 出力データの形式が明確に定義されているか（.mcp.json, .gitignore）
- [x] 処理フローが具体的に記述されているか
- [x] 想定されるエラーケースが網羅されているか
- [x] 実行方法が明確に記述されているか
- [x] テスト方法が具体的に記述されているか
- [x] 実装の詳細（アルゴリズム）を書いていないか
